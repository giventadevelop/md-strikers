'use client';

/**
 * Placeholder for optional satellite-sync UI. Clerk + ClerkSyncUrlCleanup handle sync;
 * we keep this component so layout can mount a dedicated client boundary if needed later.
 */
export default function ClerkSatelliteSyncGate() {
  return null;
}
